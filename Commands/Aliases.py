from Commands.Simple.GetLastLog import GetLastLog

aliasForSimpleCommands = {
    'get_last_log': 'GetLastLog'
}

from Commands.Complex.RollBack import RollBack

aliasForComplexCommands = {
    'rollback_to': 'RollBack'
}
